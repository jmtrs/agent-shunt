{
  "name": "agent-shunt",
  "version": "0.1.0",
  "description": "Provider-neutral, read-only bulk context worker for coding agents",
  "type": "module",
  "bin": {
    "agent-shunt": "bin/agent-shunt.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/agent-shunt.mjs && node --check src/*.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
